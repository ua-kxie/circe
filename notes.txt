problems:
    grid: gotta use gpu instancing or something
    zooming translate to keep cursor position: seems hard, changing transforms doesnt seem to change Global transform immediately
    picking: bevy picking
    cursor: always render on top, have to undo camera zoom. can't draw on UI and keep snapping to world